# Minimal, fast, deterministic dependency set for CI (the runners use the
# pure-NumPy fast backtester, so vectorbt/optuna/torch are intentionally absent).
pandas>=2.2,<3.0
numpy>=1.26,<2.2
pyarrow>=15.0
scipy>=1.11
pydantic>=2.6,<3.0
pydantic-settings>=2.2
PyYAML>=6.0
yfinance>=0.2.40
ruff>=0.4
pytest>=8.0
